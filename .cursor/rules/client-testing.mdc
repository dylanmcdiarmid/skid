---
alwaysApply: false
---
You can run all client side tests using `bun run test:client` or filter (based on matching a part of the file name) using
`bun run test:client -- file1,file2` 

We have an example working client tests in client/src/components/data-table/data-table.test.tsx
